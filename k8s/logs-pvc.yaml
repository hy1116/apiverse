# gateway/admin 로그 보관용. 파드가 하나뿐인 단일 노드(kind) 클러스터라
# 같은 PVC를 앱별 subPath로 나눠 마운트해도 RWO 제약에 걸리지 않는다.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apiverse-logs-pvc
  namespace: apiverse
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
